-- Initialize the SummerTides SQLite database.
-- SQLite creates the database file automatically when you open a new database path.
PRAGMA foreign_keys = ON;

SELECT 'SummerTides database ready for schema creation.' AS status;
